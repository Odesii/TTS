export default class GameMenu {
    // container;
    
    // constructor(scene) {
    //     const { width }= scene.scale;

    //     this.container = scene.add.container(width - 10, 50);

    //     const panel = scene.add.nineslice(0, 0);
    // }
}